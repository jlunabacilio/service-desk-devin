Look at `git diff --cached`.
Write a Conventional Commit message:
- Format: `type(scope): subject`
- Types: feat, fix, refactor, docs, chore, test, style
- Subject: max 72 characters, imperative mood, no period at end
- If the diff spans multiple concerns, add a body with one bullet per change.

Output only the commit message. Nothing else.