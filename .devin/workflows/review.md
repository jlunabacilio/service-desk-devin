Review the files I modified in this conversation.
For each file, check:
1. Correctness — does the logic match the requirements?
2. TypeScript — no `any`, explicit return types, correct generics.
3. Styling — no inline styles, consistent with the Ant Design theme.
4. Edge cases — empty arrays, undefined props, loading states.
5. Rule compliance — check against @rules.

Output as a numbered list. Max 10 items. Lead with the most critical issue.
If nothing is wrong, say "LGTM" and stop.